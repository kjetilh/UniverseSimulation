# Relasjonell universgraf v0.15aa for ikke-spesialister

Her testet vi ikke mange nye tilfeller. Vi tok bare noen få små nabo-tilfeller rundt de tre `v15z`-eksemplene for å se om forklaringen holder litt utenfor originalcasene.

- Anker `151` med forventet `p1_compact_radius_trigger` fikk status `not_supported` og match-rate `0.000`.
- Anker `239` med forventet `fragmented_fast_tradeoff_trigger` fikk status `not_supported` og match-rate `0.000`.
- Anker `271` med forventet `p0_calm_singleton_trigger` fikk status `not_supported` og match-rate `0.000`.

Neste naturlige steg er `stop_generalizing`: Neste steg bør være en ny observabel eller et annet defect-spørsmål, ikke mer trigger-generalisering.
