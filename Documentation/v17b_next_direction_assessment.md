# v17b next direction

Formal status: `v17b_resource_not_qualified`.

Stop before stability and source-spectrum tests. Diagnose the first failed constructor layer without relaxing the frozen thresholds. If valid yield improves but displacement fails, investigate cycle-length and acceptance structure rather than merely extending the chain.
