# v16y next direction

Status: `v16y_2x2_chain_finite_centers_not_stable`.

Do not add source spectra or observed-effect statistics. Do not simply double the same 2x2-chain budget yet: seed and early/late stability already pass, while all failures are start-family failures and the two endpoint clouds remain much farther apart than either cloud's internal spread.

The next narrow gate should be `v16z_alternating_cycle_bridge_gate`, effect-blind on the same six coarse state spaces and the same source/random-cost start pairs. Decompose each symmetric difference into valid alternating cycles, report cycle-length and changed-edge coverage, test exact forward/reverse whole-cycle moves, and attempt bounded 2x2 bridge searches. A failed bounded search must remain `unresolved`, not be called proof of disconnection.

Only if a reversible longer-cycle move class is qualified should a new finite start/seed/time stability comparison be preregistered. The v16s spectrum effect remains closed until a global probability law has passed this accessibility layer.
