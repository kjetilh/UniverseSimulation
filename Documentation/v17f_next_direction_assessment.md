# v17f next direction

Formal status: `v17f_finite_movement_not_qualified`.

The formal failure stands: do not run the matched-work start-memory comparison and do not rescue the proposal with a larger step or search budget.

The descriptive postrun replay localizes the only failed movement condition to `11` reverse-unsupported raw auxiliaries across `9/24` chains. All `11` explicit reverse paths were structurally valid and all exhausted the frozen witness-search budget; all other movement floors passed `24/24`. The smallest next gate is therefore a probability-law repair, not a scale increase:

1. retain the frozen batch-4, length-5 and 20,000-state generation law;
2. define valid length-5 support as generated auxiliaries whose mapped reverse auxiliary is also supported under that same frozen law;
3. turn non-paired raw auxiliaries into proposal dead ends before valid-yield accounting;
4. require exact final-endpoint and accepted-transition parity with v17f, zero runtime reverse-unsupported events, representation `12/12`, movement `24/24` and resource `24/24`;
5. keep source spectrum and observed effects closed.

Only a qualified reverse-closed repair may proceed to a separate matched accepted-edge-work start-memory gate. One-step length novelty remains distinct from connected-component novelty.
