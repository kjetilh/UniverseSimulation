# v17a next direction

Formal status: `v17a_cycle_proposal_finite_movement_not_qualified`.

Do not run a start/seed/time effect comparison. Diagnose the first failed v17a layer. Repair only representation or reverse-support instrumentation failures; if exact pathwise balance passes but finite movement fails, retire this bounded cycle proposal rather than increasing budget automatically.
