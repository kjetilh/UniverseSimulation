# Operativ anbefaling v0.16k

Status: `v16k_instrumentation_failed`.

Primary median effect ratio: `8.592671`.

Longer-perturbation median ratio: `8.768244`.

Magnitude class: `compatible_with_both_prior_anchors`.

Frozen overall: keep the instrumentation failure. The six primary-arm runs
have a valid component pass (`192/192` primary perturbations; median ratio
`8.592671`), but v16k is not a completed replication.

Next: qualify the strict perturbation sampler on the saved v16k DAGs using
completion behavior only, then freeze it for a new 12-run fresh holdout. Do not
proceed directly to a more constrained resource-aware null. Do not fit
dimension or claim geometry.
